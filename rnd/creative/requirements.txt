# Core (needed for the mock test suite — already in the cos venv)
pandas>=2.0
requests>=2.31
python-dotenv>=1.0
openai>=1.40
pydantic>=2.0
pytest>=8.0
pillow>=10.1            # compositor (draw/auto-fit/scrim/logo) + image handling
matplotlib>=3.7         # brain EDA plots + DejaVu Sans fallback font for the compositor

# Live generation only (lazy-imported; install before a real run)
fal-client>=0.5         # the ONLY generation provider: FLUX.2 + Bria (image) + Seedance (video)

# Optional: a sharper saliency model for text placement (Pillow edge-density is the default)
# opencv-python-headless>=4.9
