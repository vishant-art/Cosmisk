> **Status: ♻️ SUPERSEDED (2026-05-31)** — earliest (Apr-26) codebase audit. Superseded by `19_05/audit.md`.
> _Body unchanged; status added in the 31_05 dev_reports consolidation. Terms per `dev_reports/VOCABULARY.md`._
> _Body compressed 2026-06-17 (volume reduction): redundant restatement removed; unique essence retained below. Full original in git history; live restatement in the successor doc(s) above._

# Cosmisk Codebase Audit — 2026-04-26

## Unique essence preserved
- Baseline: branch `analysis-and-cleanup` · commit `69b4352`. Graph: 290 files · 3,369 nodes · 33,360 edges (TS/JS/Bash). 19 communities, 927 cross-community edges, 8 high-coupling warnings.
- Top modules by size (community index): #7 `tests-it:returns` 920 (coh 0.11, `server/src/__tests__`); #17 `sprint-detail-load` 518 (0.26, `src/app/features`); #19 `pipes-it:should` 470 (0.15, `src/app/shared`); #16 `services-it:should` 408 (0.13, `src/app/core`); #13 `services-generate` 247 (0.13, `server/src/services`). Backend `services-generate` hotspot: 387 edges→routes, 51→db, 66→backend tests, 45→utils/safe. Frontend cohesion (~0.26) > backend (0.11–0.13).
- **Risk 1 — DB/SQLite (= Risk #1, confirmed):** `db/index.ts` better-sqlite3, single shared connection, `journal_mode=WAL`, `foreign_keys=ON` (concurrent reads, serialized writes). `db/schema.ts` = **35 tables, ~17 indexes**. 15 tables with NO secondary index → full scans on `user_id`/`email`: users, meta_tokens, google_tokens, campaigns, ugc_projects, ugc_concepts, ugc_scripts, subscriptions, user_usage, automations, creative_sprints, agent_core_memory, tiktok_tokens, score_predictions, activity_log. `config.ts` has unwired `databaseUrl` Postgres slot. SQLite-on-disk pins to a single replica (no horizontal scale / zero-downtime deploy / read replicas; weak backup). Direction: managed Postgres + Drizzle, pooling, index pass, query-plan review.
- **Risk 2 — visibility (confirmed, understated):** `utils/logger.ts` Pino stdout-only, no Sentry/Datadog/remote aggregation. **222 server try/catch** (orig claim 108, >2×). **85 `console.*`** alongside **116 `logger.*`** → split stacks; `console.*` bypasses Pino JSON in prod. Slack webhook in config but no central error→Slack/Sentry sink. No request-id/correlation-id propagation. SIGINT/SIGTERM graceful shutdown exists; no unhandledRejection/uncaughtException reporting. Direction: Sentry on server + browser, request-id Fastify hook, replace `console.*` calls, central error → Slack sink for fatals.
- **Risk 3 — as-any (partly confirmed):** **104 total `as any`** (88 server + 16 frontend), not 74. ~53/88 server in `__tests__`; ~35 production DB-row casts: `audit/index.ts` 7, `routes/audits.ts` 4, `routes/creative-studio.ts` 3, `routes/automations.ts` 3, `routes/ad-accounts.ts` 3, `services/audit-scheduler.ts` 3. `strict:true` already on (root + server tsconfig). **229 Zod usages** (HTTP-boundary validation active). The original '12 critical' framing is roughly right in shape — the worst casts pretend a `db.prepare(...).get()` row matches a domain type → silent corruption, not at HTTP edges. Direction: typed row interfaces / Drizzle inferred models, prod casts→0, keep test casts.
- **Already healthy:** @fastify/cors|helmet|rate-limit|jwt registered in `index.ts`; per-route limits /auth/login 10/min, /auth/signup 5/min, /auth/forgot-password 3/min; Razorpay webhook HMAC verify `routes/billing.ts:412`; `config.ts` boot guard refuses default JWT_SECRET/tokenEncryptionKey in prod; whitelisted-column dynamic SQL in `routes/content.ts` + `routes/billing.ts` (no injection); sprint-restart `recoverInterruptedSprints()` in `services/job-queue.ts`; 81 test files; `.github/workflows/ci.yml` present.

## Pointer
- SUPERSEDED → see: `19_05/audit.md` (carries the open-findings restatement + god-files/as-any counts).
