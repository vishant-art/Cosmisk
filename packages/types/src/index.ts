// @cosmisk/types — shared contracts across apps (web ↔ api ↔ worker).
// Bucket H scaffold (Phase 1): empty on purpose. Migrate duplicated cross-app
// types here incrementally (Phase 6), starting with the intelligence-layer
// outputs (THE ONE THING / client cards) so they're typed once end-to-end.
export {};
