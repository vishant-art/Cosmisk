{
  "$schema": "https://turbo.build/schema.json",
  "//": "Bucket H: pipeline definitions. Inert until `turbo` is installed + the workspace is wired (Phase 2+). TS apps/packages only; the Python apps/ai-service builds/tests via uv outside this graph.",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    }
  }
}
