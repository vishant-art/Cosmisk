{
  "$schema": "https://turbo.build/schema.json",
  "//": "Bucket H: task pipelines for the TS workspace (apps/web + packages/*). apps/api is excluded from npm workspaces and builds via its own scripts. Run via root scripts (npm run build|test|typecheck|lint -> turbo run).",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    }
  }
}
